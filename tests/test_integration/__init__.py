"""
Integration Tests for Proratio Trading System

This module contains integration tests that verify end-to-end functionality
of critical system workflows:
- Data pipeline (collection → storage → loading)
- Signal-to-trade workflow (signal generation → trade creation → trade management)

These tests use real component instances (not mocks) to test actual interactions.
"""
